@using WebApp.Models.Helpers
@model Entities_POJO.UserProfile

@{
    ViewBag.Title = "EnvioOTP";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-6">
        @using (Html.BeginForm("EnvioOTPCorreo", "Home", FormMethod.Post))
        {
            <div class="container">
                <div class="row">
                    <div class="col-md-4">
                    </div>
                    <div class="col-md-4">
                        <form class="form-signin">
                            <h1 class="h3 mb-3 font-weight-normal">Envío por Correo</h1>
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true)
                            @if (@ViewBag.Message != null)
                            {
                                <div style="border: 1px solid red">
                                    @ViewBag.Message
                                </div>
                            }
                            <div class="form-group">
                                @Html.LabelFor(a => a.UserName)
                                @Html.TextBoxFor(a => a.UserName)
                            </div>
                            <div class="form-group">
                                @Html.LabelFor(a => a.Password)
                                @Html.TextBoxFor(a => a.Password)
                            </div>

                            <button class="btn btn-lg btn-primary" type="submit">Iniciar</button>
                        </form>
                    </div>
                    <div class="col-md-4">
                    </div>
                </div>
            </div>}

    </div>
    <div class="col-md-6">
        @using (Html.BeginForm("EnvioOTPTelefono", "Home", FormMethod.Post))
        {
            <div class="container">
                <div class="row">
                    <div class="col-md-4">
                    </div>
                    <div class="col-md-4">
                        <form class="form-signin">
                            <h1 class="h3 mb-3 font-weight-normal">Envío por SMS</h1>
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true)
                            @if (@ViewBag.Message != null)
                            {
                                <div style="border: 1px solid red">
                                    @ViewBag.Message
                                </div>
                            }
                            <div class="form-group">
                                @Html.LabelFor(a => a.UserName)
                                @Html.TextBoxFor(a => a.UserName)
                            </div>
                            <div class="form-group">
                                @Html.LabelFor(a => a.Password)
                                @Html.TextBoxFor(a => a.Password)
                            </div>

                            <button class="btn btn-lg btn-primary" type="submit">Iniciar</button>
                        </form>
                    </div>
                    <div class="col-md-4">
                    </div>
                </div>
            </div>}
    </div>
</div>

